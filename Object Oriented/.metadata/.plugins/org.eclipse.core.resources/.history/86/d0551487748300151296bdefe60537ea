package io;

import java.io.File;
import java.util.ArrayList;

import sudoku.Board;
import sudoku.Iterator;

public class Import {

	public static Board nextBoard(File file){
		ArrayList<String> board = new ArrayList<String>();
		board.add("1234");
		board.add("5678");
		board.add("90AB");
		board.add("CDEF");
		Board b = new Board(4, board);
		Iterator iter = b.getIterator();
		while(iter.hasNext())
			System.out.println(iter.next());
		return board;
	}
}
